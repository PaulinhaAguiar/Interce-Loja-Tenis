import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    title: {
        fontSize: 24,
        marginVertical: '2%',
        fontFamily: 'Anton_400Regular',
        paddingHorizontal: '2%'
    }
});